[project]
name = "teaching"
version = "0.1.0"
description = "A playground for making animations and visual experiments."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "manim>=0.20.1",
]
